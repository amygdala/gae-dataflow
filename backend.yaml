service: backend
runtime: custom
env: flex

manual_scaling:
  instances: 1

# runtime: python
# env: flex
# entrypoint: gunicorn -b :$PORT main_df:app

# runtime_config:
#   python_version: 2

# env_variables:
#     GOOGLE_APPLICATION_CREDENTIALS: 'aju-vtests3-f573035097a1.json'
